import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

const LiveConversationScreen = () => {
  return (
    <View>
      <Text>LiveConversationScreen</Text>
    </View>
  );
};

export default LiveConversationScreen;

const styles = StyleSheet.create({});
